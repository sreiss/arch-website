extends ../layouts/default

block content
    .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
        article
            if !data.page
                h2 Page invalide.
            else
                header
                    h1= data.page.title
                    .post
                        != data.page.content